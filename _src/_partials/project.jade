.item(id= "project-" + index)
	.heading
		h5.title.is-5= project.title
		h6.subtitle.is-6
		if project.url
			span #[a(href=project.url) Check out a demo]
		if project.repo
			span #[a(href=project.repo) view the source]
	.content
		each p in project.description
			p= p
		div.heading
			h6.is-6 Skills demonstrated
		ul
			each skill in project.skills
				li= skill